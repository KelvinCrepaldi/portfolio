import styled from "styled-components";

export const NavBarButton = styled.div`
  button {
    width: 40px;
    height: 40px;

    margin: 5px 10px;
    padding: 0px;

    border: none;
    background: none;
    color: white;
    font-size: 36px;
  }

  button:focus {
    color: rgb(200, 200, 255);
  }
`;
